//array js
//array is a data structure that can hold multiple values at once. It is a collection of elements, each identified by an index or key. Arrays can be of fixed size or dynamic size, depending on the programming language being used. They are commonly used to store and manipulate lists of data, such as numbers, strings, or objects.

let arr = [1, 2, 3, 4, 5];
console.log(arr); // Output: [1, 2, 3, 4, 5]
console.log(arr[0]) // Output: 1

let fruits = ["Apple", "Banana", "Cherry"];

for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i]);
}